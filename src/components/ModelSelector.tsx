
import React from "react";

// ModelSelector component removed UI since it's now handled elsewhere
const ModelSelector: React.FC<{ selectedModel: string; setSelectedModel: (m: string) => void }> = () => {
  return null;
};

export default ModelSelector;

